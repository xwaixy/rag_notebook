FROM node:22-alpine

WORKDIR /app

# Fail fast during image build if the development image cannot run npm.
RUN if ! command -v npm >/dev/null 2>&1; then apk add --no-cache npm; fi \
    && node --version \
    && npm --version

EXPOSE 80
